spring:
  application:
    name: hhs-backend
  profiles:
    active: dev
  # Spring Boot 文件上传配置
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2MB

server:
  port: 8082
  shutdown: graceful

logging:
  level:
    root: INFO

# 文件上传配置
file:
  upload:
    path: ./uploads
